// Placeholder that lets openchat_profile exist before its first real source
// does. The profile units add their files to the library from
// cmake/profile/*.cmake; the finishing unit deletes this file when it folds
// those fragments into CMakeLists.txt.

namespace OpenChat {
}
